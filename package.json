{
    "name": "webstack-hugo-on-edgeone",
    "private": true,
    "scripts": {
      "prebuild": "rm -rf exampleSite/themes/webstack-hugo && mkdir -p exampleSite/themes/webstack-hugo && tar --exclude='./exampleSite' --exclude='./node_modules' --exclude='./.git' --exclude='./.github' -cf - . | tar -C exampleSite/themes/webstack-hugo -xf -",
      "build": "hugo --minify -s exampleSite",
      "dev": "hugo server -D -s exampleSite",
      "clean": "rm -rf exampleSite/public",
      "postinstall": "git submodule update --init || true"
    },
    "devDependencies": {
      "hugo-extended": "^0.129.0"
    }
  }
  